# پروژه تشخیص بخش‌های واکدار، بی‌واک و سکوت در سیگنال صوتی

[🇬🇧 English](README.md) | [🇮🇷 فارسی](README_FA.md)

این پروژه شامل تحلیل و پردازش سیگنال‌های صوتی برای شناسایی بخش‌های مختلف گفتار است.

## ساختار پروژه

### بخش 1: دریافت و پردازش اولیه سیگنال صوتی

- **part1a_read_audio.py**: خواندن و نمایش فایل صوتی
- **part1b_short_term_energy.py**: محاسبه و نمایش انرژی کوتاه‌مدت

### بخش 2: تشخیص بخش‌های واکدار و بی‌واک و سکوت

- **part2a_frame_segmentation.py**: تقسیم سیگنال به فریم‌های 20 میلی‌ثانیه‌ای
- **part2b_zcr_calculation.py**: محاسبه ZCR (Zero Crossing Rate)
- **part2c_classification.py**: طبقه‌بندی بر اساس ZCR و انرژی
- **part2d_autocorrelation.py**: استفاده از اتوکرولیشن برای تشخیص واکدار
- **part2e_combined_method.py**: ترکیب روش‌ها برای بهبود تشخیص
- **generate_report.py**: تولید گزارش Word با تمام مراحل و تصاویر

## نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

**کتابخانه‌های مهم برای نمایش فارسی:**
- **arabic-reshaper**: برای تبدیل و شکل‌دهی صحیح حروف فارسی/عربی
- **python-bidi**: برای نمایش صحیح متن راست‌به‌چپ (RTL) در Word

این کتابخانه‌ها برای نمایش صحیح متن فارسی در گزارش Word استفاده می‌شوند.

## نحوه استفاده

1. فایل صوتی با نام `audio.flac` در همان پوشه قرار دارد (اولویت با FLAC است)
2. هر فایل را به ترتیب اجرا کنید:

```bash
python part1a_read_audio.py
python part1b_short_term_energy.py
python part2a_frame_segmentation.py
python part2b_zcr_calculation.py
python part2c_classification.py
python part2d_autocorrelation.py
python part2e_combined_method.py

# تولید گزارش Word 
python generate_report.py
```

**نکته**: اگر فایل صوتی موجود نباشد، هر فایل به صورت خودکار یک سیگنال نمونه ایجاد می‌کند.

## خروجی‌ها

هر فایل نمودارهای مربوطه را در فایل‌های PNG ذخیره می‌کند:
- `part1a_audio_display.png` - نمایش سیگنال صوتی در دامنه زمانی و فرکانسی
- `part1b_short_term_energy.png` - انرژی کوتاه‌مدت و دامنه RMS
- `part2a_frame_segmentation.png` - تقسیم‌بندی فریم‌ها
- `part2b_zcr_calculation.png` - تغییرات ZCR در طول زمان
- `part2c_classification.png` - طبقه‌بندی بر اساس ZCR و انرژی
- `part2d_autocorrelation.png` - تحلیل اتوکرولیشن
- `part2e_combined_method.png` - نتایج روش ترکیبی
- `part2e_final_result.png` - نتیجه نهایی با رنگ‌بندی بخش‌ها

### درباره فایل‌های تصویری (PNG)

**توجه مهم**: تمام فایل‌های PNG موجود در این پروژه از فایل صوتی ضمیمه شده (`audio.flac`) تولید شده‌اند. این تصاویر نتایج تحلیل و پردازش همان فایل صوتی هستند.

**برای کاربران و مشارکت‌کنندگان**:
- اگر فایل صوتی `audio.flac` را با فایل صوتی خودتان جایگزین کنید، با اجرای مجدد اسکریپت‌های پردازش، تمام تصاویر PNG به‌طور خودکار بر اساس فایل صوتی جدید تولید می‌شوند.
- هر فایل صوتی ویژگی‌های منحصر به فرد خود را دارد، بنابراین تصاویر تولید شده برای فایل‌های مختلف متفاوت خواهند بود.
- برای مشاهده نتایج با فایل صوتی خودتان، کافی است فایل `audio.flac` را جایگزین کرده و تمام اسکریپت‌ها را دوباره اجرا کنید.

## توضیحات فنی

### انرژی کوتاه‌مدت
انرژی هر فریم به صورت مجموع مربعات نمونه‌ها محاسبه می‌شود.

### ZCR (Zero Crossing Rate)
نرخ عبور از صفر نشان‌دهنده تعداد تغییرات علامت در یک فریم است و برای تشخیص بی‌واک مفید است.

### اتوکرولیشن
اتوکرولیشن برای شناسایی تناوب در سیگنال استفاده می‌شود و می‌تواند فرکانس پایه (F0) را برای بخش‌های واکدار تشخیص دهد.

### طبقه‌بندی
- **سکوت**: انرژی پایین
- **بی‌واک**: انرژی متوسط، ZCR بالا، اتوکرولیشن ضعیف
- **واکدار**: انرژی بالا، ZCR پایین، اتوکرولیشن قوی

## تولید گزارش Word

### معرفی اسکریپت `generate_report.py`

این اسکریپت یک گزارش کامل و حرفه‌ای با فرمت Word ایجاد می‌کند که برای ارسال به استاد مناسب است.

### ویژگی‌های گزارش‌ساز:

1. **نمایش صحیح متن فارسی**:
   - استفاده از کتابخانه `arabic-reshaper` برای شکل‌دهی صحیح حروف فارسی
   - استفاده از `python-bidi` برای نمایش راست‌به‌چپ (RTL)
   - فونت 'B Nazanin' برای نمایش زیبای متن فارسی

2. **محتوای کامل**:
   - صفحه عنوان با تاریخ
   - فهرست مطالب
   - تمام مراحل پروژه با توضیحات تشریحی
   - تمام تصاویر و نمودارها با زیرنویس
   - نتیجه‌گیری

3. **ساختار حرفه‌ای**:
   - فرمت‌بندی مناسب
   - راست‌چین برای متن فارسی
   - مرکزچین برای تصاویر
   - استفاده از عنوان‌های سلسله‌مراتبی

### نحوه استفاده:

پس از اجرای تمام فایل‌های پردازش، گزارش را ایجاد کنید:

```bash
# ابتدا تمام فایل‌های پردازش را اجرا کنید
python part1a_read_audio.py
python part1b_short_term_energy.py
python part2a_frame_segmentation.py
python part2b_zcr_calculation.py
python part2c_classification.py
python part2d_autocorrelation.py
python part2e_combined_method.py

# سپس گزارش Word را ایجاد کنید
python generate_report.py
```

### خروجی:

فایل گزارش با نام `گزارش_پروژه_پردازش_سیگنال_صوتی.docx` در همان پوشه ذخیره می‌شود.

**نکته مهم**: برای نمایش صحیح متن فارسی در Word، فونت 'B Nazanin' باید در سیستم شما نصب باشد. اگر این فونت نصب نیست، می‌توانید:
- فونت را از اینترنت دانلود و نصب کنید
- یا در فایل `generate_report.py` نام فونت را به فونت فارسی دیگری که در سیستم شما نصب است تغییر دهید (مثل 'Tahoma' یا 'Arial')

## فایل‌های مستندات

- **README.md**: راهنمای استفاده به زبان انگلیسی (پیش‌فرض GitHub)
- **README_FA.md**: راهنمای استفاده به زبان فارسی (این فایل)
- **README_EN.md**: راهنمای کامل به زبان انگلیسی
- **explanations.md**: توضیحات تشریحی کامل فارسی
- **PROJECT_DESCRIPTION.md**: توضیحات پروژه برای رزومه و لینکدین

